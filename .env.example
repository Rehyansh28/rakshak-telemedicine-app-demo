# Cloudflare Tunnel token (Zero Trust → Networks → Tunnels → your tunnel → Install connector)
# In the tunnel's Public Hostname tab, route sachin.software → http://frontend:5555
CLOUDFLARE_TUNNEL_TOKEN=

# Public domain served through the tunnel
PUBLIC_DOMAIN=sachin.software
